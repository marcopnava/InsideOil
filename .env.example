# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/insideoil"

# NextAuth
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
NEXTAUTH_URL="https://insideoil.it"

# App
NEXT_PUBLIC_APP_URL="https://insideoil.it"

# Stripe (https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."

# Seed admin password (used only by prisma/seed.ts)
ADMIN_SEED_PASSWORD="your-strong-admin-password"
